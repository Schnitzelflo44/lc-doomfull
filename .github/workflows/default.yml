name: Build LC-Doom Mod

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main
  workflow_dispatch:

env:
  DOTNET_NOLOGO: true
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages
  PLUGIN_SDK_CACHE: ${{ github.workspace }}/.plugins/cache

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.101
          cache: true
          cache-dependency-path: src/src/packages.lock.json  # Use for LethalCompany.Doom
          global-json-file: global.json

      - name: Restore Dependencies for LethalCompany.Doom
        run: dotnet restore src/src/LethalCompany.Doom.csproj

      - name: Build LethalCompany.Doom
        run: dotnet build src/src/LethalCompany.Doom.csproj -c Release

      - name: Publish LethalCompany.Doom
        run: dotnet publish src/src/LethalCompany.Doom.csproj -c Release --no-restore

      - name: Restore Dependencies for ManagedDoom
        run: dotnet restore src/ManagedDoom/ManagedDoom.csproj

      - name: Build ManagedDoom
        run: dotnet build src/ManagedDoom/ManagedDoom.csproj -c Release

      - name: Publish ManagedDoom
        run: dotnet publish src/ManagedDoom/ManagedDoom.csproj -c Release --no-restore

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lc-doom
          path: '**/*.dll'  # Adjust this to include any specific output files you want
